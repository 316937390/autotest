#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (1.0.0-dev)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import TSSLSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

from protocol.thrift.advanced_search import RecommendSrv
from protocol.thrift.advanced_search.ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print('')
  print('Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] [-s[sl]] function [arg1 [arg2...]]')
  print('')
  print('Functions:')
  print('  QueryResult getCampaigns(QueryParam paramList)')
  print('  void ModDict(DictMeta param)')
  print('   getDictMeta()')
  print('  string getValue(string dict_name, string key)')
  print('  RuntimeInfo getRuntimeInfo()')
  print('  string memoryDetail(string table, string key)')
  print('  i32 UpdateDataToMem(string table)')
  print('  Status status()')
  print('')
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
ssl = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

if sys.argv[argi] == '-s' or sys.argv[argi] == '-ssl':
  ssl = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSSLSocket.TSSLSocket(host, port, validate=False) if ssl else TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = RecommendSrv.Client(protocol)
transport.open()

if cmd == 'getCampaigns':
  if len(args) != 1:
    print('getCampaigns requires 1 args')
    sys.exit(1)
  pp.pprint(client.getCampaigns(eval(args[0]),))

elif cmd == 'ModDict':
  if len(args) != 1:
    print('ModDict requires 1 args')
    sys.exit(1)
  pp.pprint(client.ModDict(eval(args[0]),))

elif cmd == 'getDictMeta':
  if len(args) != 0:
    print('getDictMeta requires 0 args')
    sys.exit(1)
  pp.pprint(client.getDictMeta())

elif cmd == 'getValue':
  if len(args) != 2:
    print('getValue requires 2 args')
    sys.exit(1)
  pp.pprint(client.getValue(args[0],args[1],))

elif cmd == 'getRuntimeInfo':
  if len(args) != 0:
    print('getRuntimeInfo requires 0 args')
    sys.exit(1)
  pp.pprint(client.getRuntimeInfo())

elif cmd == 'memoryDetail':
  if len(args) != 2:
    print('memoryDetail requires 2 args')
    sys.exit(1)
  pp.pprint(client.memoryDetail(args[0],args[1],))

elif cmd == 'UpdateDataToMem':
  if len(args) != 1:
    print('UpdateDataToMem requires 1 args')
    sys.exit(1)
  pp.pprint(client.UpdateDataToMem(args[0],))

elif cmd == 'status':
  if len(args) != 0:
    print('status requires 0 args')
    sys.exit(1)
  pp.pprint(client.status())

else:
  print('Unrecognized method %s' % cmd)
  sys.exit(1)

transport.close()
